{
  "name": "openvscan",
  "version": "1.0.0",
  "private": true,
  "description": "OpenVScan - Open-source vulnerability scanner with AI-assisted analysis",
  "scripts": {
    "prebuild": "pnpm --filter @openvscan/db build && pnpm --filter @openvscan/types build && pnpm --filter @openvscan/github build",
    "dev": "pnpm dev:web",
    "dev:web": "pnpm --filter openvscan-web dev",
    "build": "pnpm prebuild && pnpm --filter openvscan-web build",
    "build:web": "pnpm --filter openvscan-web build",
    "deploy": "pnpm prebuild && pnpm --filter openvscan-web exec wrangler deploy",
    "db:generate": "pnpm --filter @openvscan/db generate",
    "db:migrate": "pnpm --filter openvscan-web exec wrangler d1 migrations apply openvscan --remote",
    "db:migrate:local": "pnpm --filter openvscan-web exec wrangler d1 migrations apply openvscan --local"
  },
  "keywords": [
    "vulnerability-scanner",
    "security",
    "ai",
    "openvscan"
  ],
  "author": "Buddhsen Tripathi",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/Buddhsen-tripathi/openvscan.git"
  },
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=8.0.0"
  }
}
